<!doctype html>
<html lang="en" class="no-js">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="theme-color" content="#050505" />
  <meta name="description" content="Abu Hurera — AI Workflow Systems Architect, founder of Shalcon Intelligence. Websites, automation systems, AI agents and digital products." />
  <title>Abu Hurera — AI Workflow Systems Architect</title>
  <link href="https://fonts.googleapis.com/css2?family=Anton&family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />
  <style>
:root {
  --bg: #050505;
  --bg-soft: #0a0a0a;
  --fg: #f0efe9;
  --muted: #9b9b98;
  --line: rgba(240, 239, 233, 0.18);
  --line-strong: rgba(240, 239, 233, 0.44);
  --display: "Anton", Impact, Haettenschweiler, "Arial Narrow Bold", sans-serif;
  --sans: "Inter", Arial, Helvetica, sans-serif;
  --pad: clamp(20px, 4vw, 64px);
  --ease: cubic-bezier(.2,.75,.25,1);
  --header-h: 88px;
}

* { box-sizing: border-box; }
html { background: var(--bg); color: var(--fg); scroll-behavior: smooth; }
body { margin: 0; min-width: 320px; background: var(--bg); color: var(--fg); font-family: var(--sans); overflow-x: hidden; -webkit-font-smoothing: antialiased; }
body.menu-open { overflow: hidden; }
a { color: inherit; text-decoration: none; }
button { color: inherit; font: inherit; }
img { max-width: 100%; display: block; }
::selection { background: #efeee8; color: #050505; }

.skip-link { position: fixed; left: 14px; top: 14px; z-index: 10000; padding: 10px 14px; background: var(--fg); color: var(--bg); transform: translateY(-200%); transition: transform .2s ease; }
.skip-link:focus { transform: translateY(0); }

.film-grain { position: fixed; inset: -50%; z-index: 9000; pointer-events: none; opacity: .055; mix-blend-mode: screen; background-image: repeating-radial-gradient(circle at 0 0, rgba(255,255,255,.8) 0 1px, transparent 1px 3px); background-size: 7px 7px; animation: grain 260ms steps(2,end) infinite; }
.film-grain.is-off { opacity: 0; }
@keyframes grain { 0%{transform:translate3d(-1%,1%,0)} 25%{transform:translate3d(1%,-1%,0)} 50%{transform:translate3d(.5%,1%,0)} 75%{transform:translate3d(-.7%,-.3%,0)} 100%{transform:translate3d(0,0,0)} }
.bloom { position: fixed; z-index: 0; inset: 0; pointer-events: none; background: radial-gradient(circle at 50% 38%, rgba(255,255,255,.055), transparent 34%); }
.scroll-progress { position: fixed; top: 0; left: 0; right: 0; height: 2px; z-index: 9500; background: transparent; }
.scroll-progress span { display: block; width: 100%; height: 100%; transform: scaleX(0); transform-origin: left center; background: var(--fg); }

.loader { position: fixed; inset: 0; z-index: 9998; background: #000; display: grid; place-items: center; }
.loader__frame { position: relative; width: min(90vw, 1180px); height: min(80vh, 760px); border: 1px solid rgba(255,255,255,.09); overflow: hidden; }
.loader__eyebrow, .loader__counter { position: absolute; top: 24px; font-size: 10px; letter-spacing: .22em; color: rgba(255,255,255,.62); }
.loader__eyebrow { left: 24px; }
.loader__counter { right: 24px; font-variant-numeric: tabular-nums; }
.loader__word { position: absolute; left: 4%; top: 20%; overflow: hidden; line-height: .86; }
.loader__word--right { left: auto; right: 4%; top: 52%; }
.loader__word span { display: block; font-family: var(--display); font-size: clamp(92px, 18vw, 280px); letter-spacing: -.04em; transform: translateY(110%); }
.loader__line { position: absolute; left: 24px; right: 24px; bottom: 28px; height: 1px; background: rgba(255,255,255,.15); transform-origin: left; transform: scaleX(0); }

.site-header { position: fixed; top: 74px; left: 0; right: 0; z-index: 7000; height: var(--header-h); padding: 0 var(--pad); display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; transition: background .5s var(--ease), border-color .5s var(--ease); border-bottom: 1px solid transparent; }
.site-header.is-scrolled { top:0; background: rgba(5,5,5,.74); backdrop-filter: blur(12px); border-bottom-color: rgba(255,255,255,.08); }
.wordmark { justify-self: start; font-family: var(--display); font-size: clamp(20px, 2vw, 29px); letter-spacing: .02em; line-height: 1; }
.desktop-nav { display: flex; align-items: center; gap: clamp(34px, 7vw, 104px); }
.desktop-nav a { position: relative; font-size: 14px; color: rgba(255,255,255,.82); padding: 9px 0; }
.desktop-nav a::after { content:""; position:absolute; left:0; right:0; bottom:3px; height:1px; background:var(--fg); transform:scaleX(0); transform-origin:right; transition:transform .35s var(--ease); }
.desktop-nav a:hover::after,.desktop-nav a.is-active::after { transform:scaleX(1); transform-origin:left; }
.header-actions { justify-self: end; display: flex; align-items: center; gap: 14px; }
.grain-toggle { appearance: none; border: 0; background: transparent; padding: 9px; cursor: pointer; }
.grain-toggle__track { width: 54px; height: 28px; display: block; border-radius: 999px; background: var(--fg); padding: 3px; transition: background .3s ease; }
.grain-toggle__thumb { width: 22px; height: 22px; display: block; margin-left: 26px; border-radius: 50%; background: var(--bg); transition: margin .35s var(--ease), background .35s ease; }
.grain-toggle[aria-pressed="false"] .grain-toggle__track { background: rgba(255,255,255,.28); }
.grain-toggle[aria-pressed="false"] .grain-toggle__thumb { margin-left: 0; background: var(--fg); }
.menu-toggle { display: none; width: 42px; height: 42px; border: 1px solid var(--line); background: transparent; position: relative; cursor: pointer; }
.menu-toggle span { position: absolute; left: 10px; right: 10px; height: 1px; background: var(--fg); transition: transform .35s var(--ease), top .35s var(--ease); }
.menu-toggle span:first-child { top: 16px; }
.menu-toggle span:last-child { top: 24px; }
.menu-toggle[aria-expanded="true"] span:first-child { top: 20px; transform: rotate(45deg); }
.menu-toggle[aria-expanded="true"] span:last-child { top: 20px; transform: rotate(-45deg); }

.mobile-menu { position: fixed; inset: 0; z-index: 6500; background: #050505; display: none; padding: 120px var(--pad) 32px; transform: translateY(-100%); visibility: hidden; }
.mobile-menu nav { display: grid; border-top: 1px solid var(--line); }
.mobile-menu nav a { display: grid; grid-template-columns: 48px 1fr; align-items: baseline; border-bottom: 1px solid var(--line); padding: 16px 0; font-family: var(--display); font-size: clamp(56px, 15vw, 92px); line-height: 1; letter-spacing: -.03em; }
.mobile-menu nav a span { font-family: var(--sans); font-size: 10px; color: var(--muted); letter-spacing: .12em; }
.mobile-menu__meta { position: absolute; left: var(--pad); right: var(--pad); bottom: 28px; display: flex; justify-content: space-between; gap: 20px; font-size: 10px; letter-spacing: .08em; color: var(--muted); text-transform: uppercase; }

.cursor { position: fixed; left: 0; top: 0; z-index: 9999; width: 18px; height: 18px; border: 1px solid rgba(255,255,255,.75); border-radius: 50%; pointer-events: none; transform: translate(-50%,-50%); mix-blend-mode: difference; opacity: 0; }
.cursor span { position: absolute; inset: 5px; border-radius: inherit; background: #fff; opacity: .7; }
.cursor.is-active { width: 42px; height: 42px; border-color: #fff; }

.scene { position: relative; z-index: 1; }
.section-kicker { display: flex; gap: 18px; align-items: center; font-size: 10px; text-transform: uppercase; letter-spacing: .16em; color: var(--muted); }
.section-kicker span:first-child { color: var(--fg); }

.hero { height: 180vh; background: #050505; }
.hero__stage { position: sticky; top: 0; height: 100svh; min-height: 640px; overflow: hidden; isolation: isolate; }
.hero__stage::after { content:""; position:absolute; inset:0; z-index:5; pointer-events:none; background: linear-gradient(180deg,rgba(0,0,0,.18),transparent 22%,transparent 70%,rgba(0,0,0,.5)); }
.hero__ghost-title { position:absolute; inset:0; z-index:1; display:grid; place-items:center; margin:0; font-weight:400; transform:translateY(-4.2vh); pointer-events:none; }
.hero__ghost-row { width:max-content; overflow:visible; text-align:center; white-space:nowrap; position:absolute; left:50%; display:flex; align-items:center; gap:.16em; transform:translateX(-50%) scaleX(.51); transform-origin:center; }
.hero__ghost-row span { display:inline-block; font-family:var(--display); font-size:clamp(210px,29vw,470px); line-height:.80; letter-spacing:-.035em; color:#f0f0ec; text-shadow:0 0 18px rgba(255,255,255,.16); transform-origin:center; }
.hero__portrait-wrap { position:absolute; inset:0; z-index:3; pointer-events:none; }
.hero__portrait-reveal { position:absolute; inset:0; overflow:hidden; clip-path:inset(100% 0 0 0); }
.hero__portrait-plane { position:absolute; left:50%; top:50%; width:max(100vw,133.334svh); aspect-ratio:4/3; transform:translate(-50%,-50%); }
.hero__portrait-motion { position:absolute; inset:0; transform-origin:50% 58%; will-change:transform; filter:drop-shadow(0 12px 20px rgba(0,0,0,.35)); }
.hero__portrait-source { width:100%; height:100%; object-fit:contain; max-width:none; user-select:none; }
.hero__role { position: absolute; z-index: 6; bottom: 21.8vh; margin: 0; font-size: clamp(11px, 1.25vw, 18px); color: rgba(255,255,255,.82); letter-spacing: -.01em; }
.hero__role--left { left: var(--pad); }
.hero__role--right { right: var(--pad); text-align: right; }
.hero__socials { position: absolute; z-index: 6; left: var(--pad); bottom: 7.4vh; display: flex; gap: 12px; }
.icon-link { display: grid; place-items: center; width: 42px; height: 42px; border-radius: 50%; background: rgba(255,255,255,.11); border: 1px solid rgba(255,255,255,.08); transition: background .25s ease, transform .25s ease; }
.icon-link:hover { background: rgba(255,255,255,.2); transform: translateY(-2px); }
.icon-link svg { width: 18px; height: 18px; fill: var(--fg); }
.scroll-cue { position: absolute; z-index: 6; left: 50%; bottom: 4.5vh; transform: translateX(-50%); display: flex; align-items: center; gap: 10px; font-size: 9px; letter-spacing: .18em; color: rgba(255,255,255,.52); }
.scroll-cue i { width: 48px; height: 1px; background: rgba(255,255,255,.28); position: relative; overflow: hidden; }
.scroll-cue i::after { content:""; position:absolute; inset:0; background:#fff; transform:translateX(-100%); animation: cue 1.7s var(--ease) infinite; }
@keyframes cue { 55%,100%{transform:translateX(100%)} }
.hero__corner { position:absolute; z-index:6; width:16px; height:16px; opacity:.28; }
.hero__corner--tl { top: 116px; left: var(--pad); border-left:1px solid #fff; border-top:1px solid #fff; }
.hero__corner--br { right:var(--pad); bottom:32px; border-right:1px solid #fff; border-bottom:1px solid #fff; }

.manifesto { min-height: 340vh; background: #060606; }
.manifesto__pin { position: sticky; top: 0; height: 100svh; overflow: hidden; padding: calc(var(--header-h) + 28px) var(--pad) 38px; display: flex; flex-direction: column; justify-content: space-between; isolation: isolate; }
.manifesto__pin::before { content:""; position:absolute; inset:0; background: radial-gradient(circle at 70% 55%, rgba(255,255,255,.04), transparent 28%); z-index:-2; }
.manifesto__meta { display:flex; justify-content:space-between; gap:20px; font-size:10px; text-transform:uppercase; letter-spacing:.16em; color:var(--muted); }
.manifesto__title { margin:0; width:100%; z-index:2; }
.manifesto__line { display:block; overflow:hidden; line-height:.82; }
.manifesto__line b { display:block; width:max-content; font-family:var(--display); font-size:clamp(92px,15.6vw,246px); letter-spacing:-.04em; font-weight:400; transform:translateY(105%); }
.manifesto__line:nth-child(2) b { margin-left:12vw; }
.manifesto__line:nth-child(3) b { margin-left:3vw; }
.manifesto__line--outline b { color:transparent; -webkit-text-stroke:1px rgba(255,255,255,.9); margin-left:44vw; }
.manifesto__support { display:flex; flex-wrap:wrap; gap:8px 28px; z-index:3; max-width:680px; font-size:13px; color:rgba(255,255,255,.68); }
.manifesto__portrait-echo { position:absolute; z-index:1; right:-3vw; bottom:-32vh; width:min(64vw,900px); opacity:.14; filter:blur(.25px) contrast(1.06); clip-path:inset(8% 0 0 40%); }
.manifesto__portrait-echo img { width:100%; max-width:none; }

.work-intro { min-height: 130vh; padding: 16vh var(--pad) 18vh; overflow:hidden; display:grid; align-content:center; grid-template-columns:minmax(0,1fr) minmax(260px,420px); gap:40px; background:#050505; color:var(--fg); }
.work-intro .section-kicker { position:absolute; top:calc(var(--header-h) + 34px); left:var(--pad); color:var(--muted); }
.work-intro .section-kicker span:first-child { color:var(--fg); }
.work-intro h2 { margin:0; font-family:var(--display); font-size:clamp(84px,12.6vw,210px); line-height:.84; letter-spacing:-.045em; font-weight:400; }
.work-intro h2 span { color:transparent; -webkit-text-stroke:1px rgba(240,239,233,.82); }
.work-intro p { align-self:end; margin:0 0 2vh; max-width:370px; font-size:14px; line-height:1.7; color:var(--muted); }
.work-intro__marquee { position:absolute; left:0; bottom:3.5vh; width:100%; overflow:hidden; white-space:nowrap; border-top:1px solid var(--line); border-bottom:1px solid var(--line); padding:10px 0; font-size:10px; letter-spacing:.22em; }
.work-intro__marquee span { display:inline-block; min-width:200%; }

.project { position:relative; height:190vh; background:#050505; color:var(--fg); }
.project__sticky { position:sticky; top:0; height:100svh; min-height:640px; overflow:hidden; padding:calc(var(--header-h) + 38px) var(--pad) 42px; isolation:isolate; }
.project__sticky::before { content:""; position:absolute; inset:0; z-index:-4; background:linear-gradient(180deg,#080808,#030303); }
.project__number { position:absolute; left:var(--pad); top:calc(var(--header-h) + 8px); font-size:10px; letter-spacing:.16em; color:var(--muted); }
.project__number::before { content:"PROJECT "; }
.project__content { position:absolute; z-index:5; left:var(--pad); bottom:9vh; width:min(64vw,980px); }
.project__content--right { left:auto; right:var(--pad); width:min(60vw,930px); text-align:right; }
.project__eyebrow { margin:0 0 16px; font-size:10px; text-transform:uppercase; letter-spacing:.17em; color:var(--muted); }
.project h3 { margin:0; font-family:var(--display); font-weight:400; font-size:clamp(84px,13.3vw,218px); line-height:.79; letter-spacing:-.043em; }
.project__summary { width:min(460px,90%); margin:22px 0 18px; font-size:13px; line-height:1.7; color:rgba(255,255,255,.68); }
.project__content--right .project__summary { margin-left:auto; }
.project__meta-grid { display:flex; flex-wrap:wrap; gap:8px 22px; font-size:10px; text-transform:uppercase; letter-spacing:.1em; color:rgba(255,255,255,.46); }
.project__content--right .project__meta-grid { justify-content:flex-end; }
.text-link { display:inline-flex; align-items:center; gap:12px; margin-top:24px; padding-bottom:8px; border-bottom:1px solid rgba(255,255,255,.45); font-size:12px; }
.text-link span { transition:transform .35s var(--ease); }
.text-link:hover span { transform:translate(3px,-3px); }

.project--shalcon .project__backtype { position:absolute; inset:15vh -4vw auto; z-index:-1; display:grid; gap:0; transform:rotate(-5deg); opacity:.1; }
.project--shalcon .project__backtype span { font-family:var(--display); font-size:clamp(120px,20vw,320px); line-height:.73; letter-spacing:-.05em; white-space:nowrap; }
.pipeline { position:absolute; top:29vh; right:var(--pad); width:34vw; height:1px; background:rgba(255,255,255,.12); z-index:2; }
.pipeline i { position:absolute; inset:0; transform-origin:left; transform:scaleX(.04); background:#fff; }
.pipeline b { position:absolute; top:50%; width:7px; height:7px; border-radius:50%; border:1px solid #fff; background:#050505; transform:translate(-50%,-50%) scale(.5); }
.pipeline b:nth-of-type(1){left:0}.pipeline b:nth-of-type(2){left:34%}.pipeline b:nth-of-ty