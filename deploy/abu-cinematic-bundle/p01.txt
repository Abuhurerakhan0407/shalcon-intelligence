pe(3){left:68%}.pipeline b:nth-of-type(4){left:100%}

.project--madrasa .project__sticky::before { background:linear-gradient(100deg,#030303 0 52%,#0a0a0a 52%); }
.ledger-lines { position:absolute; inset:0 50% 0 0; display:grid; grid-template-columns:repeat(6,1fr); opacity:.2; z-index:-1; }
.ledger-lines i { border-right:1px solid rgba(255,255,255,.16); transform-origin:bottom; transform:scaleY(.2); }
.madrasa-index { position:absolute; left:var(--pad); bottom:9vh; z-index:2; display:grid; gap:8px; font-family:var(--display); font-size:clamp(34px,4.8vw,82px); line-height:.88; color:transparent; -webkit-text-stroke:1px rgba(255,255,255,.18); }

.project--pagevelope .project__sticky { background:#050505; color:var(--fg); }
.project--pagevelope .project__sticky::before { background:linear-gradient(145deg,#0b0b0b,#030303); }
.project--pagevelope .project__number,.project--pagevelope .project__eyebrow { color:var(--muted); }
.project--pagevelope .project__summary { color:rgba(255,255,255,.68); }
.project--pagevelope .project__meta-grid { color:rgba(255,255,255,.46); }
.project--pagevelope .text-link { border-color:rgba(255,255,255,.45); }
.mail-frame { position:absolute; right:5vw; top:15vh; width:min(40vw,620px); aspect-ratio:1.18; border:1px solid rgba(0,0,0,.36); transform:rotate(4deg) translateX(14%); opacity:.74; }
.mail-frame__top { height:34px; border-bottom:1px solid rgba(255,255,255,.16); display:flex; align-items:center; gap:7px; padding:0 10px; }
.mail-frame__top span { width:6px;height:6px;border-radius:50%;background:#f0efe9;opacity:.42; }
.mail-frame__canvas { padding:9%; display:grid; gap:10%; height:calc(100% - 34px); }
.mail-frame__canvas i { border:1px solid rgba(255,255,255,.20); transform-origin:left; transform:scaleX(.6); }
.pagevelope-words { position:absolute; right:var(--pad); bottom:6vh; display:flex; gap:2.4vw; font-family:var(--display); font-size:clamp(48px,7vw,112px); color:transparent; -webkit-text-stroke:1px rgba(255,255,255,.18); }

.project--factory .project__sticky::before { background:radial-gradient(circle at 15% 15%,#131313,#040404 44%); }
.factory-log { position:absolute; left:var(--pad); top:18vh; display:grid; gap:9px; font-family:ui-monospace,SFMono-Regular,Menlo,monospace; font-size:10px; letter-spacing:.12em; color:rgba(255,255,255,.28); }
.factory-log span { transform:translateX(-14px); opacity:.2; }
.factory-stamp { position:absolute; left:-3vw; bottom:-6vh; z-index:-1; font-family:var(--display); font-size:clamp(220px,36vw,580px); line-height:.72; color:transparent; -webkit-text-stroke:1px rgba(255,255,255,.08); letter-spacing:-.06em; }

.capabilities { min-height:170vh; padding:15vh var(--pad) 18vh; background:#070707; overflow:hidden; }
.capabilities>.section-kicker { margin-bottom:11vh; }
.capabilities__head { display:grid; grid-template-columns:1.6fr .7fr; gap:40px; align-items:end; margin-bottom:10vh; }
.capabilities__head h2 { margin:0; font-family:var(--display); font-weight:400; font-size:clamp(80px,12vw,200px); line-height:.8; letter-spacing:-.04em; }
.capabilities__head p { margin:0 0 1.5vh; max-width:340px; font-size:13px; line-height:1.7; color:var(--muted); }
.capabilities__list { position:relative; z-index:2; border-top:1px solid var(--line); }
.capability { width:100%; appearance:none; border:0; border-bottom:1px solid var(--line); background:transparent; display:grid; grid-template-columns:58px 1fr minmax(220px,.7fr); gap:18px; align-items:center; padding:20px 0; text-align:left; cursor:pointer; transition:padding .4s var(--ease), background .4s ease; }
.capability span { font-size:10px; color:var(--muted); }
.capability b { font-family:var(--display); font-weight:400; font-size:clamp(34px,5.4vw,86px); letter-spacing:-.025em; line-height:.9; }
.capability em { justify-self:end; max-width:340px; font-style:normal; font-size:12px; line-height:1.5; color:var(--muted); opacity:.35; transform:translateX(14px); transition:opacity .35s ease,transform .35s var(--ease); }
.capability:hover,.capability.is-active { padding-left:12px; padding-right:12px; background:rgba(255,255,255,.025); }
.capability:hover em,.capability.is-active em { opacity:1; transform:none; }
.capabilities__ghost { position:absolute; right:-1vw; top:34vh; z-index:0; font-family:var(--display); font-size:31vw; line-height:.7; color:transparent; -webkit-text-stroke:1px rgba(255,255,255,.035); transform:rotate(90deg) translateX(50%); transform-origin:right top; }

.about { min-height:160vh; background:#050505; color:var(--fg); overflow:hidden; padding:18vh var(--pad) 14vh; display:grid; grid-template-columns:.9fr 1.1fr; align-items:center; }
.about__backtype { position:absolute; inset:5vh auto auto -1vw; z-index:0; display:grid; font-family:var(--display); font-size:clamp(180px,28vw,440px); line-height:.7; letter-spacing:-.055em; color:transparent; -webkit-text-stroke:1px rgba(255,255,255,.08); }
.about__portrait { position:relative; z-index:1; align-self:end; width:min(58vw,880px); margin-left:-12vw; margin-bottom:-16vh; clip-path:inset(7% 0 0 33%); filter:contrast(1.02); }
.about__portrait::after { content:""; position:absolute; inset:0; background:linear-gradient(180deg,transparent 60%,#050505 95%); pointer-events:none; }
.about__portrait img { width:100%; max-width:none; }
.about__copy { position:relative; z-index:2; max-width:680px; justify-self:end; padding-top:20vh; }
.about .section-kicker { color:var(--muted); margin-bottom:5vh; }
.about .section-kicker span:first-child { color:var(--fg); }
.about h2 { margin:0 0 40px; font-family:var(--display); font-weight:400; font-size:clamp(68px,9.2vw,150px); line-height:.82; letter-spacing:-.04em; }
.about p { margin:0 0 20px; max-width:540px; font-size:14px; line-height:1.8; color:var(--muted); }
.about__stack { margin-top:54px; display:flex; flex-wrap:wrap; gap:8px; }
.about__stack span { padding:8px 10px; border:1px solid var(--line); font-size:10px; text-transform:uppercase; letter-spacing:.09em; }

.interlude { height:220vh; background:#050505; overflow:hidden; }
.interlude__track { position:sticky; top:0; height:100svh; display:flex; align-items:center; gap:4vw; width:max-content; padding-left:8vw; will-change:transform; }
.interlude__track span { font-family:var(--display); font-size:clamp(180px,29vw,470px); letter-spacing:-.05em; line-height:.8; color:transparent; -webkit-text-stroke:1px rgba(255,255,255,.74); }
.interlude__track span:nth-child(even) { color:#f0efe9; -webkit-text-stroke:0; }
.interlude__center { position:absolute; left:50%; top:50%; transform:translate(-50%,-50%); z-index:3; width:min(72vw,660px); padding:26px; background:rgba(5,5,5,.76); backdrop-filter:blur(8px); border:1px solid var(--line); text-align:center; }
.interlude__center p { margin:0 0 14px; font-size:14px; line-height:1.6; }
.interlude__center span { font-size:9px; letter-spacing:.16em; color:var(--muted); }

.contact { min-height:115svh; padding:16vh var(--pad) 0; background:#020202; overflow:hidden; display:flex; flex-direction:column; justify-content:space-between; }
.contact__background { position:absolute; left:50%; bottom:-4vh; transform:translateX(-50%); white-space:nowrap; font-family:var(--display); font-size:clamp(190px,31vw,510px); line-height:.7; letter-spacing:-.05em; color:transparent; -webkit-text-stroke:1px rgba(255,255,255,.07); }
.contact__content { position:relative; z-index:2; max-width:1200px; }
.contact__kicker { display:block; margin-bottom:6vh; font-size:10px; text-transform:uppercase; letter-spacing:.17em; color:var(--muted); }
.contact h2 { margin:0; font-family:var(--display); font-weight:400; font-size:clamp(82px,13.6vw,220px); line-height:.79; letter-spacing:-.045em; }
.contact h2 span { color:transparent; -webkit-text-stroke:1px #f0efe9; }
.contact__content>p { max-width:500px; margin:28px 0 36px; color:var(--muted); font-size:13px; line-height:1.7; }
.contact__actions { width:min(820px,100%); border-top:1px solid var(--line); }
.contact-link { display:flex; justify-content:space-between; align-items:center; padding:16px 0; border-bottom:1px solid var(--line); font-size:14px; }
.contact-link b { font-weight:400; transition:transform .35s var(--ease); }
.contact-link:hover b { transform:translate(3px,-3px); }
.site-footer { position:relative; z-index:3; margin-top:14vh; min-height:72px; border-top:1px solid var(--line); display:grid; grid-template-columns:1fr auto 1fr; align-items:center; font-size:10px; letter-spacing:.12em; text-transform:uppercase; color:var(--muted); }
.site-footer span:first-child { font-family:var(--display); font-size:16px; color:var(--fg); letter-spacing:.02em; }
.site-footer>a { justify-self:end; }

:focus-visible { outline:2px solid var(--fg); outline-offset:4px; }
.project--pagevelope :focus-visible,.about :focus-visible,.work-intro :focus-visible { outline-color:var(--fg); }

.no-js .loader { display:none; }
.no-js .hero__portrait-reveal { clip-path:none; }
.no-js .manifesto__line b { transform:none; }

@media (max-width: 1100px) {
  :root { --header-h: 76px; }
  .desktop-nav { gap: 42px; }
  .hero__role { bottom: 17vh; }
  .work-intro { grid-template-columns:1fr; }
  .work-intro p { justify-self:end; }
  .project__content { width:72vw; }
  .project__content--right { width:70vw; }
  .pagevelope-words { display:grid; gap:0; text-align:right; }
  .capabilities__head { grid-template-columns:1fr; }
  .capabilities__head p { justify-self:end; }
  .about { grid-template-columns:.8fr 1.2fr; }
}

@media (max-width: 820px) {
  :root { --pad: 20px; --header-h: 68px; }
  .site-header { top:12px; }
  .site-header.is-scrolled { top:0; }
  .site-header { grid-template-columns:1fr auto; }
  .desktop-nav,.grain-toggle { display:none; }
  .menu-toggle { display:block; }
  .mobile-menu { display:block; }
  .cursor { display:none; }
  .hero { height:145vh; }
  .hero__stage { min-height:600px; }
  .hero__ghost-title { place-items:start center; padding-top:27svh; transform:none; }
  .hero__ghost-row { width:100%; white-space:normal; overflow:visible; position:static; left:auto; display:flex; flex-direction:column; align-items:center; gap:0; transform:none; }
  .hero__ghost-row span { width:max-content; text-align:center; font-size:clamp(96px,29vw,150px); line-height:.72; max-width:none; transform:scaleX(.80); }
  .hero__portrait-plane { width:133.334svh; }
  .hero__portrait-motion { transform-origin:50% 54%; }
  .hero__role { bottom:11.5vh; font-size:10px; max-width:42vw; line-height:1.45; }
  .hero__role--right { right:20px; }
  .hero__role--left { left:20px; }
  .hero__socials { bottom:3.5vh; left:20px; }
  .scroll-cue { right:20px; left:auto; transform:none; bottom:4.2vh; }
  .hero__corner { display:none; }

  .manifesto { min-height:230vh; }
  .manifesto__pin { padding-top:calc(var(--header-h) + 32px); }
  .manifesto__meta { font-size:9px; }
  .manifesto__meta span:last-child { display:none; }
  .manifesto__line b { font-size:clamp(70px,23vw,128px); line-height:.9; }
  .manifesto__line:nth-child(2) b { margin-left:5vw; }
  .manifesto__line:nth-child(3) b { margin-left:0; }
  .manifesto__line--outline b { margin-left:20vw; }
  .manifesto__support { max-width:300px; gap:6px 14px; font-size:11px; }
  .manifesto__portrait-echo { width:120vw; right:-38vw; bottom:-14vh; clip-path:inset(13% 0 0 43%); opacity:.12; }

  .work-intro { min-height:100svh; padding:18vh 20px 15vh; display:flex; flex-direction:column; justify-content:center; }
  .work-intro h2 { font-size:clamp(72px,23vw,120px); }
  .work-intro p { font-size:12px; max-width:290px; align-self:flex-end; }

  .project { height:145vh; }
  .project__sticky { min-height:600px; padding:calc(var(--header-h) + 28px) 20px 30px; }
  .project__content,.project__content--right { left:20px; right:20px; bottom:7vh; width:auto; text-align:left; }
  .project h3 { font-size:clamp(72px,21vw,128px); }
  .project__summary { width:92%; font-size:11.5px; margin-top:16px; }
  .project__content--right .project__summary { margin-left:0; }
  .project__content--right .project__meta-grid { justify-content:flex-start; }
  .project__meta-grid { font-size:8.5px; gap:5px 14px; }
  .project__eyebrow { font-size:8.5px; }
  .text-link { font-size:11px; margin-top:18px; }
  .pipeline { top:22vh; right:20px; width:48vw; }
  .project--shalcon .project__backtype { top:20vh; }
  .project--shalcon .project__backtype span { font-size:28vw; }
  .madrasa-index { top:17vh; bottom:auto; left:20px; font-size:14vw; opacity:.55; }
  .project--madrasa .project__sticky::before { background:linear-gradient(180deg,#040404,#0b0b0b); }
  .ledger-lines { inset:0; grid-template-columns:repeat(4,1fr); opacity:.12; }
  .mail-frame { width:68vw; top:16vh; right:-9vw; }
  .pagevelope-words { top:18vh; bottom:auto; left:20px; right:auto; font-size:13vw; text-align:left; opacity:.72; }
  .factory-log { left:20px; top:16vh; }
  .factory-stamp { font-size:70vw; left:-6vw; bottom:20vh; }

  .capabilities { min-height:auto; padding:13vh 20px; }
  .capabilities>.section-kicker { margin-bottom:8vh; }
  .capabilities__head { margin-bottom:8vh; }
  .capabilities__head h2 { font-size:clamp(70px,20vw,110px); }
  .capabilities__head p { justify-self:start; font-size:11.5px; }
  .capability { grid-template-columns:32px 1fr; gap:8px; padding:16px 0; }
  .capability b { font-size:clamp(34px,10vw,58px); }
  .capability em { grid-column:2; justify-self:start; max-width:250px; font-size:10px; opacity:.6; transform:none; }
  .capability:hover,.capability.is-active { padding-left:0; padding-right:0; background:transparent; }
  .capabilities__ghost { display:none; }

  .about { min-height:125svh; padding:14vh 20px 10vh; display:block; }
  .about__backtype { top:9vh; font-size:38vw; }
  .about__portrait { position:absolute; left:-37vw; bottom:-7vh; width:130vw; margin:0; opacity:.26; clip-path:inset(11% 0 0 38%); }
  .about__copy { padding-top:18vh; max-width:none; }
  .about h2 { font-size:clamp(66px,18vw,104px); }
  .about p { max-width:470px; font-size:12px; }
  .about__stack { margin-top:38px; max-width:560px; }

  .interlude { height:160vh; }
  .interlude__track { padding-left:8vw; gap:8vw; }
  .interlude__track span { font-size:58vw; }
  .interlude__center { width:82vw; padding:20px; }
  .interlude__center p { font-size:12px; }

  .contact { min-height:100svh; padding:13vh 20px 0; }
  .contact h2 { font-size:clamp(72px,19vw,112px); }
  .contact__content>p { font-size:11.5px; margin:22px 0 28px; max-width:360px; }
  .contact-link { padding:14px 0; font-size:12px; }
  .site-footer { grid-template-columns:1fr 1fr; gap:6px; padding:14px 0; min-height:76px; }
  .site-footer>span:nth-child(2) { justify-self:end; }
  .site-footer>a { grid-column:1 / -1; justify-self:start; }
  .contact__background { font-size:56vw; }
}

@media (max-width: 430px) {
  .loader__frame { width:calc(100vw - 28px); height:74vh; }
  .loader__word span { font-size:38vw; }
  .hero__ghost-row span { font-size:30vw; }
  .hero__portrait-plane { width:133.334svh; }
  .hero__role { bottom:13.5vh; max-width:38vw; }
  .hero__socials { bottom:4vh; }
  .scroll-cue { bottom:5vh; }
  .manifesto__line b { font-size:22vw; }
  .work-intro h2 { font-size:22vw; }
  .project h3 { font-size:20vw; }
  .project__summary { width:100%; }
  .capability b { font-size:9.5vw; }
  .about h2 { font-size:17vw; }
  .contact h2 { font-size:18.2vw; }
}

@media (hover:none), (pointer:coarse) { .cursor { display:none; } }

@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior:auto; }
  *,*::before,*::after { animation-duration:.001ms !important; animation-iteration-count:1 !important; transition-duration:.001ms !important; }
  .loader { display:none !important; }
  .hero { height:100svh; }
  .hero__portrait-reveal { clip-path:none !important; }
  .manifesto { min-height:100svh; }
  .manifesto__line b { transform:none !important; }
  .project { height:100svh; }
  .interlude { height:100svh; overflow:hidden; }
  .interlude__track { position:absolute; transform:none !important; }
}
