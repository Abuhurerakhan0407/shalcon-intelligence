# Server-side destination for estimator/audit lead submissions.
# Never expose these values to browser code and never commit real secrets.
LEAD_WEBHOOK_URL=

# Shared secret sent only in the server-to-server X-Shalcon-Webhook-Secret header.
# Production requires at least 24 characters; use a generated random secret.
LEAD_WEBHOOK_SECRET=
